# چطور محصول را ببینیم؟ - راهنمای سریع

**تاریخ:** 2025-01-XX

---

## 🚀 راه‌اندازی سریع (5 دقیقه)

### مرحله 1: بررسی پیش‌نیازها

```bash
# بررسی نسخه Node.js (باید 20+ باشد)
node --version

# بررسی npm
npm --version
```

### مرحله 2: نصب وابستگی‌ها

```bash
# اگر هنوز نصب نکرده‌اید
npm install
```

### مرحله 3: تنظیم فایل .env

```bash
# کپی کردن فایل نمونه
cp .env.example .env
```

سپس فایل `.env` را باز کنید و تنظیمات را وارد کنید:

```env
# حداقل این موارد را تنظیم کنید:
DATABASE_URL="postgresql://user:password@host:5432/database"
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"
NEXT_PUBLIC_USE_MOCK_API="true"
```

**نکته:** برای شروع سریع، می‌توانید از Mock API استفاده کنید (بدون نیاز به دیتابیس).

### مرحله 4: راه‌اندازی دیتابیس (اختیاری - برای Mock API نیاز نیست)

```bash
# فقط اگر می‌خواهید از دیتابیس واقعی استفاده کنید
npm run db:generate
npm run db:migrate
npm run db:seed
```

### مرحله 5: اجرای سرور توسعه

```bash
npm run dev
```

### مرحله 6: باز کردن در مرورگر

بعد از اجرای دستور بالا، این پیام را می‌بینید:

```
✓ Ready in 2.5s
○ Local:        http://localhost:3000
```

**مرورگر را باز کنید و به آدرس زیر بروید:**
```
http://localhost:3000
```

---

## 📱 صفحات مختلف محصول

### 1. صفحه اصلی (Home)
```
http://localhost:3000
```
- داشبورد کاربر
- خلاصه موجودی
- آخرین تراکنش‌ها

### 2. صفحه کیف پول (Wallet)
```
http://localhost:3000/wallet
```
- نمایش موجودی
- افزایش موجودی
- برداشت
- تنظیمات افزایش خودکار

### 3. صفحه تحلیل (Analytics)
```
http://localhost:3000/analytics
```
- نمودار زمان تماشا
- نمودار هزینه
- نمودار توزیع محتوا
- خروجی CSV/PDF

### 4. صفحه اعلان‌ها (Notifications)
```
http://localhost:3000/notifications
```
- لیست اعلان‌ها
- تنظیمات اعلان‌ها

### 5. صفحه تسویه (Settlement)
```
http://localhost:3000/settlement
```
- نمای کلی تسویه
- تاریخچه تسویه
- جزئیات تسویه

### 6. صفحه تنظیمات (Settings)
```
http://localhost:3000/settings
```
- تنظیمات کاربر
- تنظیمات اعلان‌ها
- تنظیمات کیف پول

### 7. صفحه LMS (LMS Integration)
```
http://localhost:3000/lms
```
- اتصالات LMS
- مدیریت اتصالات

### 8. صفحه یکپارچه‌سازی (Integration Wizard)
```
http://localhost:3000/integration
```
- جادوگر 5 مرحله‌ای یکپارچه‌سازی

---

## 🎨 ویژگی‌های قابل مشاهده

### در صفحه اصلی:
- ✅ داشبورد با کارت‌های اطلاعاتی
- ✅ نمایش موجودی کیف پول
- ✅ آخرین تراکنش‌ها
- ✅ لینک‌های سریع

### در صفحه کیف پول:
- ✅ نمایش موجودی
- ✅ دکمه افزایش موجودی
- ✅ دکمه برداشت
- ✅ تاریخچه تراکنش‌ها
- ✅ تنظیمات افزایش خودکار

### در صفحه تحلیل:
- ✅ 5 نوع نمودار مختلف:
  - نمودار زمان تماشا (Line Chart)
  - نمودار هزینه به ازای محتوا (Bar Chart)
  - نمودار هزینه ماهانه (Area Chart)
  - نمودار هزینه بر اساس پلتفرم (Bar Chart)
  - نمودار توزیع نوع محتوا (Pie/Donut Chart)
- ✅ فیلتر تاریخ
- ✅ دکمه خروجی (Export)

### در صفحه اعلان‌ها:
- ✅ آیکون زنگ (Bell Icon) در هدر
- ✅ شمارنده اعلان‌های خوانده نشده
- ✅ لیست اعلان‌ها
- ✅ تنظیمات اعلان‌ها

---

## 🔍 تست قابلیت‌ها

### 1. تست افزایش موجودی:
1. به `/wallet` بروید
2. روی "Top-up" کلیک کنید
3. مبلغ را وارد کنید
4. روش پرداخت را انتخاب کنید
5. پرداخت را انجام دهید

### 2. تست نمودارها:
1. به `/analytics` بروید
2. بازه تاریخ را انتخاب کنید
3. روی نمودارها کلیک کنید (برای فیلتر)
4. دکمه Export را امتحان کنید

### 3. تست اعلان‌ها:
1. به `/notifications` بروید
2. اعلان‌ها را مشاهده کنید
3. تنظیمات را تغییر دهید
4. اعلان‌های جدید را ببینید

### 4. تست یکپارچه‌سازی:
1. به `/integration` بروید
2. مراحل 5 گانه را طی کنید
3. پلتفرم را انتخاب کنید
4. تنظیمات را انجام دهید

---

## 🐛 رفع مشکلات

### مشکل: سرور اجرا نمی‌شود

**راه‌حل:**
```bash
# بررسی کنید که پورت 3000 آزاد است
# یا از پورت دیگری استفاده کنید:
npm run dev -- -p 3001
```

### مشکل: خطای دیتابیس

**راه‌حل:**
```bash
# از Mock API استفاده کنید (در .env):
NEXT_PUBLIC_USE_MOCK_API="true"
```

### مشکل: صفحه سفید نمایش داده می‌شود

**راه‌حل:**
```bash
# کش را پاک کنید و دوباره اجرا کنید:
rm -rf .next
npm run dev
```

### مشکل: خطای Module Not Found

**راه‌حل:**
```bash
# وابستگی‌ها را دوباره نصب کنید:
rm -rf node_modules
npm install
```

---

## 📸 اسکرین‌شات‌ها

برای مشاهده اسکرین‌شات‌های صفحات مختلف، می‌توانید:

1. پروژه را اجرا کنید
2. به صفحات مختلف بروید
3. از صفحات اسکرین‌شات بگیرید

---

## 🎯 نکات مهم

### برای مشاهده کامل:
1. ✅ از Mock API استفاده کنید (نیازی به دیتابیس نیست)
2. ✅ همه صفحات را بررسی کنید
3. ✅ قابلیت‌های مختلف را تست کنید
4. ✅ در مرورگرهای مختلف تست کنید

### برای تست واقعی:
1. ✅ دیتابیس را راه‌اندازی کنید
2. ✅ داده‌های نمونه را وارد کنید
3. ✅ API های واقعی را تست کنید

---

## 🚀 دستورات مفید

```bash
# اجرای سرور توسعه
npm run dev

# اجرای تست‌ها
npm test

# ساخت برای Production
npm run build

# اجرای Production
npm start

# بررسی خطاهای Linter
npm run lint
```

---

## 📚 مستندات بیشتر

- [راهنمای شروع سریع](QUICK_START_GUIDE.md)
- [راهنمای توسعه‌دهنده](developer/DEVELOPER_GUIDE.md)
- [مستندات API](api/API_DOCUMENTATION.md)
- [راهنمای عملیات](operations/OPERATIONS_MANUAL.md)

---

**راهنمای مشاهده محصول** ✅  
**آماده برای مشاهده!** 🚀

