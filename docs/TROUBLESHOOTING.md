# راهنمای رفع مشکلات - Pay as Play

**تاریخ:** 2025-01-XX

---

## 🔴 مشکل: ERR_CONNECTION_REFUSED

### علت:
سرور Next.js در حال اجرا نیست یا خطایی در راه‌اندازی دارد.

### راه‌حل‌ها:

#### 1. بررسی اینکه سرور در حال اجرا است:

```bash
# در PowerShell:
netstat -ano | findstr :3000

# اگر چیزی نمایش داده نشد، سرور در حال اجرا نیست
```

#### 2. راه‌اندازی سرور:

```bash
# اطمینان حاصل کنید که در دایرکتوری پروژه هستید:
cd C:\Amir\pay-as-play-project

# راه‌اندازی سرور:
npm run dev
```

#### 3. بررسی خطاها:

اگر خطایی نمایش داده شد، آن را بررسی کنید:

**خطای رایج:**
- `Port 3000 is already in use` → پورت 3000 در حال استفاده است
- `Cannot find module` → وابستگی‌ها نصب نشده
- `Database connection error` → مشکل اتصال دیتابیس

#### 4. استفاده از پورت دیگر:

اگر پورت 3000 در حال استفاده است:

```bash
# استفاده از پورت 3001:
npm run dev -- -p 3001
```

سپس به `http://localhost:3001` بروید.

#### 5. پاک کردن کش و راه‌اندازی مجدد:

```bash
# پاک کردن کش Next.js:
rm -rf .next

# یا در PowerShell:
Remove-Item -Recurse -Force .next

# راه‌اندازی مجدد:
npm run dev
```

---

## 🔴 مشکل: Port Already in Use

### راه‌حل:

#### گزینه 1: استفاده از پورت دیگر
```bash
npm run dev -- -p 3001
```

#### گزینه 2: بستن پروسه‌ای که از پورت استفاده می‌کند

```powershell
# پیدا کردن PID پروسه:
netstat -ano | findstr :3000

# بستن پروسه (جایگزین PID با عدد واقعی):
taskkill /PID [PID] /F
```

---

## 🔴 مشکل: Cannot find module

### راه‌حل:

```bash
# حذف node_modules و package-lock.json:
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json

# نصب مجدد:
npm install
```

---

## 🔴 مشکل: Database Connection Error

### راه‌حل:

#### گزینه 1: استفاده از Mock API (ساده‌تر)

در فایل `.env`:
```env
NEXT_PUBLIC_USE_MOCK_API="true"
```

#### گزینه 2: تنظیم دیتابیس

```bash
# بررسی فایل .env:
# DATABASE_URL باید تنظیم شده باشد

# راه‌اندازی دیتابیس:
npm run db:generate
npm run db:migrate
```

---

## 🔴 مشکل: صفحه سفید نمایش داده می‌شود

### راه‌حل:

```bash
# پاک کردن کش:
Remove-Item -Recurse -Force .next

# راه‌اندازی مجدد:
npm run dev
```

---

## 🔴 مشکل: خطاهای TypeScript

### راه‌حل:

```bash
# بررسی خطاها:
npm run lint

# یا:
npx tsc --noEmit
```

---

## ✅ بررسی‌های اولیه

### 1. بررسی Node.js:
```bash
node --version
# باید 20+ باشد
```

### 2. بررسی npm:
```bash
npm --version
```

### 3. بررسی وابستگی‌ها:
```bash
# بررسی اینکه node_modules وجود دارد:
Test-Path node_modules

# اگر وجود ندارد:
npm install
```

### 4. بررسی فایل .env:
```bash
# بررسی اینکه فایل .env وجود دارد:
Test-Path .env

# اگر وجود ندارد:
Copy-Item .env.example .env
```

---

## 🚀 راه‌اندازی کامل از صفر

اگر همه چیز درست کار نمی‌کند، این مراحل را دنبال کنید:

```bash
# 1. رفتن به دایرکتوری پروژه:
cd C:\Amir\pay-as-play-project

# 2. حذف node_modules و کش:
Remove-Item -Recurse -Force node_modules
Remove-Item -Recurse -Force .next
Remove-Item package-lock.json

# 3. نصب مجدد وابستگی‌ها:
npm install

# 4. بررسی فایل .env:
# اگر وجود ندارد:
Copy-Item .env.example .env

# 5. تنظیم Mock API در .env:
# NEXT_PUBLIC_USE_MOCK_API="true"

# 6. راه‌اندازی سرور:
npm run dev
```

---

## 📞 اگر مشکل حل نشد

### بررسی لاگ‌ها:
```bash
# بررسی خروجی npm run dev
# خطاها را کپی کنید
```

### بررسی فایل‌های مهم:
- `.env` - تنظیمات محیط
- `package.json` - وابستگی‌ها
- `next.config.ts` - تنظیمات Next.js

---

## 🔍 دستورات مفید برای دیباگ

```bash
# بررسی پورت‌های در حال استفاده:
netstat -ano | findstr :3000

# بررسی پروسه‌های Node:
Get-Process -Name node

# بررسی لاگ‌های Next.js:
# در ترمینال که npm run dev را اجرا کرده‌اید

# بررسی خطاهای TypeScript:
npx tsc --noEmit

# بررسی خطاهای ESLint:
npm run lint
```

---

**راهنمای رفع مشکلات** ✅  
**امیدوارم مشکل حل شود!** 🚀

