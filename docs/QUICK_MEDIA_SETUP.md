# 🎬 راهنمای سریع اضافه کردن فایل‌های رسانه

## 📍 محل فایل‌ها

فایل‌های ویدیو و صوت خود را در این پوشه‌ها قرار دهید:

```
public/demo-media/
├── video/          ← فایل‌های ویدیو (MP4)
└── audio/          ← فایل‌های صوت (MP3)
```

## ⚡ مراحل سریع

### 1️⃣ کپی کردن فایل‌ها

فایل‌های خود را مستقیماً در این پوشه‌ها کپی کنید:
- **ویدیو:** `public/demo-media/video/your-video.mp4`
- **صوت:** `public/demo-media/audio/your-audio.mp3`

### 2️⃣ به‌روزرسانی `lib/demo-content.ts`

بعد از کپی کردن فایل، فایل `lib/demo-content.ts` را باز کنید و مسیر فایل را به‌روزرسانی کنید:

**برای ویدیو (مثلاً Netflix):**
```typescript
netflix: {
  // ... سایر فیلدها
  mediaUrl: '/demo-media/video/your-video.mp4', // ← نام فایل خود را بگذارید
},
```

**برای صوت (مثلاً Spotify):**
```typescript
spotify: {
  // ... سایر فیلدها
  mediaUrl: '/demo-media/audio/your-audio.mp3', // ← نام فایل خود را بگذارید
},
```

### 3️⃣ تست

1. سرور را restart کنید: `npm run dev`
2. به صفحه اصلی بروید
3. روی یک پلتفرم connected کلیک کنید
4. فایل شما باید پخش شود!

## 📝 مثال کامل

فرض کنید شما یک فایل ویدیو به نام `my-demo.mp4` دارید:

1. ✅ فایل را در `public/demo-media/video/my-demo.mp4` کپی کنید
2. ✅ در `lib/demo-content.ts`، خط `netflix` را پیدا کنید
3. ✅ `mediaUrl` را به این صورت تغییر دهید:
   ```typescript
   mediaUrl: '/demo-media/video/my-demo.mp4',
   ```
4. ✅ سرور را restart کنید
5. ✅ تست کنید!

## 🎯 نکات مهم

- ✅ نام فایل را ساده نگه دارید (بدون فاصله و کاراکترهای خاص)
- ✅ فرمت MP4 برای ویدیو و MP3 برای صوت پیشنهاد می‌شود
- ✅ بعد از اضافه کردن فایل، حتماً سرور را restart کنید
- ✅ مسیر فایل باید با `/demo-media/` شروع شود

## ❓ مشکل دارید؟

اگر فایل پخش نمی‌شود:
1. مطمئن شوید فایل در پوشه درست قرار دارد
2. نام فایل در `lib/demo-content.ts` را بررسی کنید
3. Console مرورگر را برای خطاها چک کنید
4. سرور را restart کنید

---

**آماده تست!** 🚀

