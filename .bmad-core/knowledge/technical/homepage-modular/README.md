# 🏠 صفحه اصلی Play and Pay - ساختار ماژولار

**پروژه:** Pay as Play  
**تاریخ ایجاد:** 2025-11-04  
**ورژن:** 1.0

---

## 📦 ساختار فایل‌ها

```
homepage-modular/
├── style.css          # استایل‌های مشترک (رنگ، چیدمان، انیمیشن)
├── main.js            # اسکریپت مشترک (سوئیچ زبان، انیمیشن)
├── index.html         # صفحه کامل (همه بخش‌ها)
├── hero.html          # بخش Hero (مستقل)
├── why.html           # بخش Why It Matters (مستقل)
├── dev.html           # بخش Developers & Businesses (مستقل)
├── cta.html           # بخش Final CTA (مستقل)
└── README.md          # این فایل
```

---

## 🎯 نحوه استفاده

### روش 1: استفاده کامل (index.html)

فایل `index.html` شامل همه بخش‌ها است و آماده استفاده است:

```bash
# باز کردن در مرورگر
open index.html  # Mac
start index.html  # Windows
```

### روش 2: استفاده ماژولار (Elementor/Webflow)

هر فایل HTML مستقل است و می‌تواند جداگانه استفاده شود:

1. **افزودن CSS و JS پایه:**
   - کپی کردن محتوای `style.css` به بخش Custom CSS
   - کپی کردن محتوای `main.js` به بخش Custom JS (قبل از `</body>`)

2. **افزودن هر بخش:**
   - ایجاد یک HTML Embed
   - کپی کردن محتوای `<section>` از فایل مربوطه
   - مثال: از `hero.html` فقط بخش `<section>` را کپی کنید

---

## 🌍 ویژگی‌های زبان

### زبان‌های پشتیبانی شده

- **فارسی (FA):** RTL - پیش‌فرض
- **انگلیسی (EN):** LTR
- **آلمانی (DE):** LTR

### تغییر زبان پیش‌فرض

در فایل `main.js`، خط زیر را تغییر دهید:

```javascript
window.__pp_lang = 'fa'; // تغییر به 'en' یا 'de'
```

### Language Switcher

سوئیچر زبان در Header قرار دارد و به صورت خودکار:
- زبان HTML را تغییر می‌دهد (`lang` attribute)
- جهت متن را تغییر می‌دهد (`dir` attribute)
- محتوای همه بخش‌ها را همزمان تغییر می‌دهد
- انتخاب را در localStorage ذخیره می‌کند

---

## 🎨 طراحی

### رنگ‌ها

- **Primary Gradient:** از `#7c8cf8` به `#2de1c2`
- **Background:** `#ffffff`
- **Card:** `#f8f9fb`
- **Border:** `#e7e9ef`
- **Text:** `#1e233a`
- **Muted Text:** `#667089`

### انیمیشن‌ها

- **Reveal Animation:** عناصر با کلاس `.reveal` به صورت تدریجی ظاهر می‌شوند
- **Hover Effects:** دکمه‌ها و لینک‌ها دارای افکت hover هستند
- **Smooth Transitions:** همه تغییرات با transition نرم انجام می‌شوند

---

## 📱 Responsive Design

### Breakpoints

- **Mobile:** < 768px
- **Tablet:** 768px - 900px
- **Desktop:** > 900px

### ویژگی‌های Mobile

- Grid به یک ستون تبدیل می‌شود
- CTA دکمه‌ها در مرکز قرار می‌گیرند
- فونت‌ها با `clamp()` تنظیم می‌شوند
- Padding و spacing کاهش می‌یابد

---

## 🔧 سفارشی‌سازی

### تغییر رنگ‌ها

در فایل `style.css`، متغیرهای CSS را تغییر دهید:

```css
:root {
  --grad: linear-gradient(135deg, #7c8cf8, #2de1c2);
  --bg: #ffffff;
  /* ... */
}
```

### اضافه کردن بخش جدید

1. یک فایل HTML جدید ایجاد کنید (مثال: `features.html`)
2. ساختار مشابه سایر بخش‌ها را استفاده کنید
3. محتوای سه‌زبانه را با کلاس `.lang` اضافه کنید
4. `data-lang` را به `<section>` اضافه کنید

---

## 📝 ساختار هر بخش

هر بخش HTML شامل:

```html
<section class="section" data-lang>
  <div class="container">
    <div class="lang is-active" data-i18n="fa">
      <!-- محتوای فارسی -->
    </div>
    <div class="lang" data-i18n="en">
      <!-- محتوای انگلیسی -->
    </div>
    <div class="lang" data-i18n="de">
      <!-- محتوای آلمانی -->
    </div>
  </div>
</section>
```

---

## ✅ Checklist استفاده

### قبل از استفاده

- [ ] فایل‌های CSS و JS را به سایت اضافه کرده‌اید
- [ ] زبان پیش‌فرض را تنظیم کرده‌اید
- [ ] لینک‌های CTA را به URLهای واقعی تغییر داده‌اید

### تست

- [ ] Language Switcher کار می‌کند
- [ ] RTL در فارسی درست نمایش داده می‌شود
- [ ] LTR در انگلیسی/آلمانی درست است
- [ ] انیمیشن‌ها کار می‌کنند
- [ ] Responsive در Mobile تست شده
- [ ] Responsive در Desktop تست شده

---

## 🚀 Host آنلاین

### GitHub Pages

1. فایل‌ها را در یک Repository آپلود کنید
2. GitHub Pages را فعال کنید
3. دسترسی از `username.github.io/repo/`

### Netlify

1. Drag & Drop پوشه `homepage-modular`
2. دریافت لینک فوری

### Vercel

1. آپلود فایل‌ها
2. دریافت لینک با CDN خودکار

---

## 📚 مستندات مرتبط

- **Website Structure:** `../references/website-structure.md`
- **Brand Messaging:** `../references/brand-messaging.md`
- **About Page:** `../about-page.html`

---

**آماده برای استفاده! 🚀**

**تاریخ:** 2025-11-04  
**ورژن:** 1.0

